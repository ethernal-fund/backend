[tool.poetry]
name = "ethernal-backend"
version = "0.1.0"
description = "Ethernal Backend API"
authors = []

[tool.poetry.dependencies]
python = "~3.12.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"